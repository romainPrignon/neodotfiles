### required cli configs

# fzf
[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh

# ctrl+e
[ -f ~/.fzf.zsh ] && bindkey '^E' fzf-file-widget

# mise
[ -f ~/.local/bin/mise ] && eval "$(~/.local/bin/mise activate zsh)"

### optional cli configs
for file in $HOME/.cli.d/*; do source $file; done
